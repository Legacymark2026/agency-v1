{
  "name": "@agency/vault-client",
  "version": "1.0.0",
  "description": "Enterprise HashiCorp Vault client with AppRole authentication, in-memory caching, and zero-downtime secret rotation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --outDir dist",
    "dev": "tsc --watch --outDir dist",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^4.1.4"
  }
}
