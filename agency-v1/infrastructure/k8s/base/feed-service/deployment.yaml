apiVersion: apps/v1
kind: Deployment
metadata:
  name: feed-service
  labels: { app: feed-service, tier: backend, priority: tier-c }
spec:
  replicas: 1
  selector:
    matchLabels: { app: feed-service }
  template:
    metadata:
      labels: { app: feed-service, tier: backend }
    spec:
      priorityClassName: production-low-priority
      containers:
        - name: feed-service
          image: docker.io/legacymark2026/feed-service:latest
          ports: [{ containerPort: 4024 }]
          resources:
            requests: { cpu: "50m", memory: "128Mi" }
            limits: { cpu: "250m", memory: "256Mi" }
          envFrom: [{ secretRef: { name: service-secrets } }]
          env:
            - { name: PORT, value: "4024" }
            - { name: NODE_ENV, value: "production" }
            - { name: NODE_OPTIONS, value: "--max-old-space-size=192" }
            - { name: OTEL_SERVICE_NAME, value: "feed-service" }
            - { name: REDIS_URL, valueFrom: { secretKeyRef: { name: redis-credentials, key: url } } }
            - { name: DATABASE_URL, valueFrom: { secretKeyRef: { name: db-credentials, key: url } } }
          livenessProbe:
            httpGet: { path: /health, port: 4024 }
            initialDelaySeconds: 45
            periodSeconds: 20
          readinessProbe:
            httpGet: { path: /health, port: 4024 }
            initialDelaySeconds: 15
            periodSeconds: 10
