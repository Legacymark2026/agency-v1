apiVersion: apps/v1
kind: Deployment
metadata:
  name: project-service
  labels: { app: project-service, tier: backend }
spec:
  replicas: 2
  selector:
    matchLabels: { app: project-service }
  template:
    metadata:
      labels: { app: project-service }
    spec:
      containers:
        - name: project-service
          image: docker.io/legacymark2026/project-service:latest
          ports: [{ containerPort: 4018 }]
          resources:
            requests: { cpu: "250m", memory: "256Mi" }
            limits: { cpu: "1000m", memory: "512Mi" }
          envFrom: [{ secretRef: { name: service-secrets } }]
          env:
            - { name: PORT, value: "4018" }
            - { name: REDIS_URL, valueFrom: { secretKeyRef: { name: redis-credentials, key: url } } }
            - { name: POSTGRES_EXTERNAL_URL, valueFrom: { secretKeyRef: { name: db-credentials, key: url } } }
          livenessProbe:
            httpGet: { path: /health, port: 4018 }
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet: { path: /ready, port: 4018 }
            initialDelaySeconds: 5
            periodSeconds: 10
