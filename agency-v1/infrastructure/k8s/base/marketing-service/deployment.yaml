apiVersion: apps/v1
kind: Deployment
metadata:
  name: marketing-service
  labels: { app: marketing-service, tier: backend }
spec:
  replicas: 2
  selector:
    matchLabels: { app: marketing-service }
  template:
    metadata:
      labels: { app: marketing-service }
    spec:
      containers:
        - name: marketing-service
          image: docker.io/legacymark2026/marketing-service:latest
          ports: [{ containerPort: 4009 }]
          resources:
            requests: { cpu: "250m", memory: "256Mi" }
            limits: { cpu: "1000m", memory: "512Mi" }
          envFrom: [{ secretRef: { name: service-secrets } }]
          env:
            - { name: PORT, value: "4009" }
            - { name: REDIS_URL, valueFrom: { secretKeyRef: { name: redis-credentials, key: url } } }
            - { name: POSTGRES_EXTERNAL_URL, valueFrom: { secretKeyRef: { name: db-credentials, key: url } } }
          livenessProbe:
            httpGet: { path: /health, port: 4009 }
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet: { path: /ready, port: 4009 }
            initialDelaySeconds: 5
            periodSeconds: 10
