apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-service
  labels: { app: chat-service, tier: backend, priority: tier-c }
spec:
  replicas: 1
  selector:
    matchLabels: { app: chat-service }
  template:
    metadata:
      labels: { app: chat-service, tier: backend }
    spec:
      priorityClassName: production-low-priority
      containers:
        - name: chat-service
          image: docker.io/legacymark2026/chat-service:latest
          ports: [{ containerPort: 4023 }]
          resources:
            requests: { cpu: "50m", memory: "128Mi" }
            limits: { cpu: "250m", memory: "256Mi" }
          envFrom: [{ secretRef: { name: service-secrets } }]
          env:
            - { name: PORT, value: "4023" }
            - { name: NODE_ENV, value: "production" }
            - { name: NODE_OPTIONS, value: "--max-old-space-size=192" }
            - { name: OTEL_SERVICE_NAME, value: "chat-service" }
            - { name: REDIS_URL, valueFrom: { secretKeyRef: { name: redis-credentials, key: url } } }
            - { name: DATABASE_URL, valueFrom: { secretKeyRef: { name: db-credentials, key: url } } }
          livenessProbe:
            httpGet: { path: /health, port: 4023 }
            initialDelaySeconds: 45
            periodSeconds: 20
          readinessProbe:
            httpGet: { path: /health, port: 4023 }
            initialDelaySeconds: 15
            periodSeconds: 10
