apiVersion: apps/v1
kind: Deployment
metadata:
  name: public-api-service
  labels: { app: public-api-service, tier: backend }
spec:
  replicas: 2
  selector:
    matchLabels: { app: public-api-service }
  template:
    metadata:
      labels: { app: public-api-service }
    spec:
      containers:
        - name: public-api-service
          image: docker.io/legacymark2026/public-api-service:latest
          ports: [{ containerPort: 4015 }]
          resources:
            requests: { cpu: "250m", memory: "256Mi" }
            limits: { cpu: "1000m", memory: "512Mi" }
          envFrom: [{ secretRef: { name: service-secrets } }]
          env:
            - { name: PORT, value: "4015" }
            - { name: REDIS_URL, valueFrom: { secretKeyRef: { name: redis-credentials, key: url } } }
            - { name: POSTGRES_EXTERNAL_URL, valueFrom: { secretKeyRef: { name: db-credentials, key: url } } }
          livenessProbe:
            httpGet: { path: /health, port: 4015 }
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet: { path: /ready, port: 4015 }
            initialDelaySeconds: 5
            periodSeconds: 10
