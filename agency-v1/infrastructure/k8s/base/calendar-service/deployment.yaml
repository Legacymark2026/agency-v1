apiVersion: apps/v1
kind: Deployment
metadata:
  name: calendar-service
  labels: { app: calendar-service, tier: backend }
spec:
  replicas: 2
  selector:
    matchLabels: { app: calendar-service }
  template:
    metadata:
      labels: { app: calendar-service }
    spec:
      containers:
        - name: calendar-service
          image: docker.io/legacymark2026/calendar-service:latest
          ports: [{ containerPort: 4008 }]
          resources:
            requests: { cpu: "250m", memory: "256Mi" }
            limits: { cpu: "1000m", memory: "512Mi" }
          envFrom: [{ secretRef: { name: service-secrets } }]
          env:
            - { name: PORT, value: "4008" }
            - { name: REDIS_URL, valueFrom: { secretKeyRef: { name: redis-credentials, key: url } } }
            - { name: POSTGRES_EXTERNAL_URL, valueFrom: { secretKeyRef: { name: db-credentials, key: url } } }
          livenessProbe:
            httpGet: { path: /health, port: 4008 }
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet: { path: /ready, port: 4008 }
            initialDelaySeconds: 5
            periodSeconds: 10
