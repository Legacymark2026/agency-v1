apiVersion: apps/v1
kind: Deployment
metadata:
  name: hr-service
  labels: { app: hr-service, tier: backend }
spec:
  replicas: 2
  selector:
    matchLabels: { app: hr-service }
  template:
    metadata:
      labels: { app: hr-service }
    spec:
      containers:
        - name: hr-service
          image: docker.io/legacymark2026/hr-service:latest
          ports: [{ containerPort: 4017 }]
          resources:
            requests: { cpu: "250m", memory: "256Mi" }
            limits: { cpu: "1000m", memory: "512Mi" }
          envFrom: [{ secretRef: { name: service-secrets } }]
          env:
            - { name: PORT, value: "4017" }
            - { name: REDIS_URL, valueFrom: { secretKeyRef: { name: redis-credentials, key: url } } }
            - { name: POSTGRES_EXTERNAL_URL, valueFrom: { secretKeyRef: { name: db-credentials, key: url } } }
          livenessProbe:
            httpGet: { path: /health, port: 4017 }
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet: { path: /ready, port: 4017 }
            initialDelaySeconds: 5
            periodSeconds: 10
