apiVersion: apps/v1
kind: Deployment
metadata:
  name: crm-service
  labels: { app: crm-service, tier: backend }
spec:
  replicas: 2
  selector:
    matchLabels: { app: crm-service }
  template:
    metadata:
      labels: { app: crm-service }
    spec:
      containers:
        - name: crm-service
          image: docker.io/legacymark2026/crm-service:latest
          ports: [{ containerPort: 4002 }]
          resources:
            requests: { cpu: "250m", memory: "256Mi" }
            limits: { cpu: "1000m", memory: "1Gi" }
          envFrom: [{ secretRef: { name: service-secrets } }]
          env:
            - { name: PORT, value: "4002" }
            - { name: REDIS_URL, valueFrom: { secretKeyRef: { name: redis-credentials, key: url } } }
            - { name: POSTGRES_EXTERNAL_URL, valueFrom: { secretKeyRef: { name: db-credentials, key: url } } }
          livenessProbe:
            httpGet: { path: /health, port: 4002 }
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet: { path: /ready, port: 4002 }
            initialDelaySeconds: 5
            periodSeconds: 10
