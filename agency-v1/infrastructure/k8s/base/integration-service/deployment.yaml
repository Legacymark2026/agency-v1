apiVersion: apps/v1
kind: Deployment
metadata:
  name: integration-service
  labels: { app: integration-service, tier: backend }
spec:
  replicas: 2
  selector:
    matchLabels: { app: integration-service }
  template:
    metadata:
      labels: { app: integration-service }
    spec:
      containers:
        - name: integration-service
          image: docker.io/legacymark2026/integration-service:latest
          ports: [{ containerPort: 4010 }]
          resources:
            requests: { cpu: "250m", memory: "256Mi" }
            limits: { cpu: "1000m", memory: "512Mi" }
          envFrom: [{ secretRef: { name: service-secrets } }]
          env:
            - { name: PORT, value: "4010" }
            - { name: REDIS_URL, valueFrom: { secretKeyRef: { name: redis-credentials, key: url } } }
            - { name: POSTGRES_EXTERNAL_URL, valueFrom: { secretKeyRef: { name: db-credentials, key: url } } }
          livenessProbe:
            httpGet: { path: /health, port: 4010 }
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet: { path: /ready, port: 4010 }
            initialDelaySeconds: 5
            periodSeconds: 10
