apiVersion: apps/v1
kind: Deployment
metadata:
  name: goldneez-rewards-service
  labels: { app: goldneez-rewards-service, tier: backend }
spec:
  replicas: 2
  selector:
    matchLabels: { app: goldneez-rewards-service }
  template:
    metadata:
      labels: { app: goldneez-rewards-service }
    spec:
      containers:
        - name: goldneez-rewards-service
          image: docker.io/legacymark2026/goldneez-rewards-service:latest
          ports: [{ containerPort: 4020 }]
          resources:
            requests: { cpu: "250m", memory: "256Mi" }
            limits: { cpu: "1000m", memory: "512Mi" }
          envFrom: [{ secretRef: { name: service-secrets } }]
          env:
            - { name: PORT, value: "4020" }
            - { name: REDIS_URL, valueFrom: { secretKeyRef: { name: redis-credentials, key: url } } }
            - { name: POSTGRES_EXTERNAL_URL, valueFrom: { secretKeyRef: { name: db-credentials, key: url } } }
          livenessProbe:
            httpGet: { path: /health, port: 4020 }
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet: { path: /ready, port: 4020 }
            initialDelaySeconds: 5
            periodSeconds: 10
