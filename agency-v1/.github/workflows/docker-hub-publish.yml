name: Publish Microservices to Docker Hub

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'services/**'
      - 'packages/**'

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    outputs:
      services: ${{ steps.changes.outputs.services }}
    steps:
      - uses: actions/checkout@v4
      - id: changes
        uses: dorny/paths-filter@v3
        with:
          filters: |
            auth-service: ['services/auth-service/**', 'packages/**']
            crm-service: ['services/crm-service/**', 'packages/**']
            automation-service: ['services/automation-service/**', 'packages/**']
            ai-engine: ['services/ai-engine/**', 'packages/**']
            inbox-service: ['services/inbox-service/**', 'packages/**']
            finance-service: ['services/finance-service/**', 'packages/**']
            api-gateway: ['services/api-gateway/**', 'packages/**']
            video-service: ['services/video-service/**', 'packages/**']
            calendar-service: ['services/calendar-service/**', 'packages/**']
            marketing-service: ['services/marketing-service/**', 'packages/**']
            integration-service: ['services/integration-service/**', 'packages/**']
            document-service: ['services/document-service/**', 'packages/**']
            agent-team-engine: ['services/agent-team-engine/**', 'packages/**']
            analytics-service: ['services/analytics-service/**', 'packages/**']
            admin-service: ['services/admin-service/**', 'packages/**']
            public-api-service: ['services/public-api-service/**', 'packages/**']

  build-and-push:
    needs: detect-changes
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    strategy:
      fail-fast: false
      matrix:
        service:
          - auth-service
          - crm-service
          - automation-service
          - ai-engine
          - inbox-service
          - finance-service
          - api-gateway
          - video-service
          - calendar-service
          - marketing-service
          - integration-service
          - document-service
          - agent-team-engine
          - analytics-service
          - admin-service
          - public-api-service
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: legacymark/${{ matrix.service }}
          tags: |
            type=sha,format=short
            type=raw,value=latest

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: services/${{ matrix.service }}/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=legacymark/${{ matrix.service }}:buildcache
          cache-to: type=registry,ref=legacymark/${{ matrix.service }}:buildcache,mode=max
